// Assuming this is part of a larger React component or similar
const LoadingScreen = () => {
  const message = "Astryx is thinking..."; // Updated loading screen text
  return (
    <div>
      <p>{message}</p>
    </div>
  );
};

export default LoadingScreen;